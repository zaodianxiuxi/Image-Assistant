# Image Assistant 开发交接

更新日期：2026-08-26

本文用于后续在当前项目基础上继续开发。文中“已确认待实现”均已得到产品确认；实现前仍应先检查工作区状态，避免覆盖用户未提交的改动。

## 项目概况

- 技术栈：React 18、TypeScript、Vite、Express 5、Node.js。
- 启动开发环境：`npm run dev`。
- 前端地址：`http://localhost:5173`；本地服务地址：`http://localhost:3001`。
- 图片上游默认模型：`gpt-image-2`；通过 SudoCode/Sudorelay 的 OpenAI 兼容接口访问。
- 当前环境变量：`SUDOCODE_API_KEY`、可选的 `SUDOCODE_BASE_URL`。不要将密钥写入前端或提交到仓库。
- 测试与构建：`npm test`、`npm run build`。

## 已完成能力

- 文生图、图像编辑与遮罩编辑。
- 最多 10 张参考图，单张最大 20 MB，总大小最大 50 MB。
- 常用输出画幅：正方形 `1024x1024`、电脑横屏 `1536x864`、手机竖屏 `864x1536`。请求会将当前选中画幅传给服务端。
- 图片主画面与历史缩略图均可打开预览；缩略图完整显示，主图在创作画布中央按比例展示。
- 深浅主题切换与基础交互动效。
- 今日提示词热榜：内置 17 条复杂中文提示词，按日期稳定随机展示 6 条，点击条目可填入提示词输入框。

## 已确认待实现：本地图片持久化

### 目标

生成或编辑成功后的图片写入当前 Windows 登录用户桌面的 `Image-Assisant` 文件夹，不使用 MinIO，也不写死用户目录。

### 路径与保存规则

- 使用 Node 的系统路径能力读取桌面目录；Windows 可用 `path.join(os.homedir(), "Desktop")`，或者更严格地通过 Windows 已知文件夹解析。不要写入固定用户名路径。
- 目标目录：`<Desktop>\\Image-Assisant`。目录不存在时创建，存在时复用。
- 文件名由后端生成，例如 `YYYYMMDD-HHmmss-随机ID.png`；绝不使用用户上传文件名或请求参数作为目标路径。
- 上游响应同时兼容 `b64_json` 与临时图片 URL：前者解码，后者由服务端下载，再统一写入本地 PNG 文件。
- 保存成功后，接口返回本地访问地址（建议 `/generated-images/:filename`），前端继续使用现有的 `result.src` 即可支持主图、缩略图、预览和下载。
- 保存失败时返回明确错误；不得返回已失效的上游临时 URL 作为降级结果。

### 服务端边界与安全

- 将“解析上游图片”“写入本地图片”“生成本地 URL”拆分为独立函数，供 `/api/images/generate` 与 `/api/images/edit` 共用。
- 仅通过固定目录暴露图片文件，例如 `express.static` 或受控的 `GET /generated-images/:filename`。
- 路由不得接受任意文件系统路径；对文件名使用白名单校验，阻止 `..`、分隔符与路径穿越。
- 本阶段不增加数据库，不做跨重启的图片历史列表，也不做自动清理策略。历史记录仍只保存在浏览器当前会话。

### 建议测试

- 目录不存在时创建，存在时可复用。
- Base64 与 URL 两类上游响应都会写为本地文件，并返回本地 URL。
- 本地静态图片路由不能访问目标目录之外的文件。
- 写入失败会返回可读错误，不返回临时 URL。

## 已确认待实现：今日提示词热榜刷新与模型补充

### 产品交互

热榜提供两个独立操作：

1. 刷新图标按钮：立刻从本地提示词池切换一批 6 条内容，不调用模型、不消耗额度。
2. “生成新灵感”按钮：由用户明确点击后调用文本模型生成 6 条新的复杂中文提示词；加载过程中禁用重复提交并展示处理中状态。

不要让普通刷新直接调用模型。这样既保证页面快速可靠，也使模型费用和等待时间可控。

### 本地刷新规则

- 当前 `src/prompt-hotlist.mjs` 已有按日稳定洗牌的 `getDailyPromptHotlist`。
- 扩展为接受刷新序号或种子：使用“日期 + 刷新次数”生成新的稳定顺序。
- 单批必须无重复；连续刷新应优先避免与上一批完全相同。
- 不改变当前页面选择的输出画幅；热榜中的推荐画幅仅作为提示。

### 文本模型接口

- 图片模型 `gpt-image-2` 不能默认当作文本模型使用。
- 新增独立环境变量，例如：

  ```ini
  SUDOCODE_TEXT_MODEL=
  ```

- 在确认供应商支持的文本模型名称和兼容端点前，不实现真实调用。预期采用服务端 `POST /api/prompts/generate`，前端绝不直接携带 API Key。
- 预计调用 OpenAI 兼容的 `/chat/completions`；实际端点和响应格式必须根据所选服务商文档验证。
- 模型提示词要求严格返回 JSON 数组，共 6 项：

  ```json
  [
    {
      "title": "中文标题",
      "category": "分类",
      "size": "1536x864",
      "prompt": "80 至 500 字的复杂中文图像提示词"
    }
  ]
  ```

- 服务端必须解析并校验返回值：中文内容、字段非空、提示词长度 80-500、画幅仅允许 `1024x1024`、`1536x864`、`864x1536`，并与内置及缓存内容去重。无效或空响应视为失败，不写入缓存。
- 模型不可用、未配置或响应异常时，仅提示“生成新灵感失败”；本地热榜刷新仍可正常使用。

### 缓存策略

- 经校验的模型提示词写入 `<Desktop>\\Image-Assisant\\prompt-cache.json`。
- 应采用原子写入或临时文件后替换，避免中断导致 JSON 损坏。
- 服务启动或首次请求时加载缓存，合并到内置提示词池；缓存重复项忽略。
- 建议限制缓存容量，例如最多保留 120 条，按创建时间淘汰最早条目，防止文件无限增长。
- 本阶段不需要后台抓取、实时爬取或数据库。

### 建议测试

- 刷新后的热榜有 6 条且无重复，刷新序号变化会切换列表。
- 未配置文本模型时，生成新灵感接口返回可读的配置错误。
- 合法模型 JSON 会通过校验并写入缓存；格式错误、超长、非中文、错误画幅与重复项会被拒绝。
- 文本模型失败不影响本地刷新功能。

## 关键文件

- `src/App.tsx`：创作页状态、热榜展示、主题、结果预览与下载。
- `src/prompt-hotlist.mjs`：内置提示词集合和稳定随机规则。
- `src/prompt-hotlist.test.mjs`：热榜数据与随机规则测试。
- `src/prompt-hotlist-ui.test.mjs`：热榜和预览相关 UI 结构测试。
- `server/index.mjs`：Express API、上游图片请求、上传校验与日志。
- `server/image-sizes.mjs`：服务端允许的图片尺寸。
- `server/upload-limits.mjs`：参考图数量及大小限制。
- `.env.example`：环境变量示例。
- `docs/今日提示词热榜.md`：现有静态提示词内容说明。
- `docs/后续方向.md`：较长期的产品方向；MinIO 相关条目不再是当前持久化方案。

## 开发约束

- 工作区当前可能存在用户未提交的 `package-lock.json` 改动；后续开发不得覆盖、回退或混入无关提交。
- 所有 Git 提交信息使用中文。
- 提交与推送前执行与改动相匹配的测试，至少执行 `npm test` 与 `npm run build`；不要声称通过而没有实际验证。
- 本地路径、API Key、上游临时 URL 和用户上传文件名均属于边界输入，必须做校验和受控处理。
